# Service Reliability Dashboard

A service reliability monitoring dashboard built with FastAPI (backend) and React + TypeScript (frontend).

Monitors external service health via periodic HTTP polling and displays status in a real-time dashboard.
